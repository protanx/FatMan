FatMan is a duplicate file manager. It finds the duplicate files and asks you if want to delete it.

it will work on the path if you give the path or not it will work on the working directory.

python fatman.py [PATH]
